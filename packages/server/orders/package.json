{
  "name": "orders",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd -r tsconfig-paths/register --respawn --transpile-only --no-notify src/server.ts"
  },
  "dependencies": {
    "@nodejs-kafka/shared": "*"
  },
  "devDependencies": {
    "tsconfig-paths": "^3.9.0"
  }
}
